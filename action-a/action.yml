name: "Hello Actions"
description: "greet someone"
author: "octocat@github.com"

inputs:
  MY_NAME:
    description: "whom to greet"
    required: true
    default: "World"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"

